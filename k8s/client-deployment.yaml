apiVersion: apps/v1
kind: Deployment
metadata:
  name: quickkart-client
spec:
  replicas: 2
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
        - name: client
          image: vikashsharma0707/quickkart-client:latest
          ports:
            - containerPort: 80